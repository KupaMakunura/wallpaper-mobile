import { View, Text } from "react-native";
import React from "react";

const AppImageModal = () => {
  return (
    <View>
      <Text>AppImageModal</Text>
    </View>
  );
};

export default AppImageModal;
